# MC 百科已知问题复核

数据源：[《通用故障排除 - 群服互联 (McBot)》](https://www.mcmod.cn/post/5072.html)，最后核对：2026-07-29。状态以 3.0.3 源码、本地测试和可重复构建为依据；“待实机复测”不等同于已向客户承诺兼容。

| # | 页面中的已知问题 | 3.0.3 处理状态 |
| --- | --- | --- |
| 1 | 局域网联机只能 QQ → 游戏，游戏 → QQ 不工作 | **待实机复测**：集成服务器聊天钩子和后台自动连接路径均存在，但缺少局域网环境与真实 OneBot 联调证据 |
| 2 | `/mcbot delGroup` 报错 | **已修复并构建**：统一 `GroupId` 参数名、Long 类型、成功提示和立即保存；旧 Fabric/Forge 同步修复 |
| 3 | `/mcbot setBot` 报错 | **已修复并构建**：正式分支修复 Brigadier Long/Integer 类型不匹配；1.7.10 修复 `Long.getLong` 误用并同步连接配置 |
| 4 | `@` 显示 `[@null]` | **已修复并测试**：群名片和昵称为空时回退到 QQ 号 |
| 5 | 自定义 `say` 命令报错 | **已修复并测试**：按位置解析多词参数，不再在遍历参数时修改原列表 |
| 6 | NeoForge/Forge 查询 TPS 丢失换行 | **已修复并测试**：命令反馈按行保留换行 |
| 7 | 1.20.1 与 Chat Image Display 联动不显示图片 | **代码路径已修复，待联调**：检测到 `chatimage` 时会发送 `CICode` 图片占位；仍需指定版本组合的真实客户端测试 |
| 8 | Spark 等复杂返回无法转发 | **文本已修复，图片化未完成**：异步输出有界等待、响应隔离、纯文本转义和分段已实现；将输出渲染为图片仍是功能请求 |
| 9 | 1.12.2 无法加载 | **未修复，外部依赖阻塞**：`OneBot-SDK:0.1.4` 与 `0.1.4-jdk8` 的原仓库文件均返回 404；没有可验证原始制品时不发布不完整 JAR |
| 10 | Carpet 假人触发崩溃 | **已修复**：进度没有展示信息时不再强取空 Optional/空展示对象 |
| 11 | 1.7.10 无法使用 | **部分完成**：Java 8 完整构建通过，并修复群号、机器人账号、数组消息、WSS、连接和重复监听；GTNH/真实服务端仍待回归 |
| 12 | 1.18.2/1.19.2 消息转发两次 | **代码级修复并构建**：群号发送前去重，重复事件注册增加幂等保护；Fabric/Forge 1.18.2 与 1.19.2 完整构建通过，仍建议实机复测 |
| 13 | 1.19.2 可能启动失败 | **代码级修复并构建**：自动连接失败不再中断服务器启动；Fabric/Forge 1.19.2 完整构建通过 |
| 14 | 框架 60 秒内未连接时游戏可能崩溃 | **已修复并构建**：OneBot 连接移至专用后台线程，停服会取消连接并关闭客户端 |
| 15 | 连接服务器时游戏可能无法加载 | **已修复并构建**：自动连接不再占用 Minecraft 服务器线程 |
| 16 | OneBot `message` 不是字符串时不显示 | **已修复并测试**：同时接受 CQ 字符串和消息段数组；1.7.10 也已回移 |
| 17 | FTB 颜色码导致 QQ 转发乱码 | **已修复并测试**：发送前移除旧式、十六进制和 ANSI 格式控制码，同时保留换行与制表符 |
| 18 | `/mcbot reload` 不能热重载配置和命令 | **已修复并构建**：重新加载配置、语言和自定义命令；正式与旧 Fabric/Forge、1.7.10 均已处理 |
| 19 | 群命令在服务端控制台没有输出 | **已修复并构建**：记录来源群、用户、命令 ID 和返回字符数，不记录令牌等敏感配置 |
| 20 | 默认地址存在时 `connect <其他地址>` 不生效 | **已修复并构建**：命令地址规范化后立即持久化，再异步连接；支持 `ws://`、`wss://` 和无协议地址 |

## 结论

不能宣称页面中的问题已经全部修复。当前明确未完成的是 1.12.2 可交付构建和复杂输出图片化；局域网、Chat Image Display、GTNH/Carpet 及旧版双转发还需要对应模组组合的真实服务器回归。其余可由代码直接定位的问题已经修复，并在相应分支通过测试或完整构建。
