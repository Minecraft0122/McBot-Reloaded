# McBot Reloaded GitHub 测试规范

- 规范版本：1.0
- 状态：生效
- 维护者：Minecraft0122
- 最后更新：2026-07-29

本规范定义 McBot Reloaded 的持续集成、兼容性验证和发布门禁。其目标是让测试任务由 GitHub Actions 在可复现的干净环境中完成，并为每次合并、巡检和发布保留可审计的结果。

本文中的“必须”表示质量门禁的强制条件；“建议”表示通常应执行、但不单独阻止合并的检查。

## 1. 适用范围

本规范适用于以下正式维护分支及其发布产物：

| Minecraft | 分支 | 加载器 | 最低加载器版本 | 构建 JDK | 必测运行 JDK |
| --- | --- | --- | --- | --- | --- |
| 1.20.1 | `1.20.1` | Fabric | 0.19.3 | 17 | 17、21、25 |
| 1.20.1 | `1.20.1` | Forge | 47.4.22 | 17 | 17、21、25 |
| 1.21.1 | `1.21.1` | Fabric | 0.19.3 | 21 | 21、25 |
| 1.21.1 | `1.21.1` | NeoForge | 21.1.244 | 21 | 21、25 |

没有列入表中的 Minecraft、加载器或 Java 组合不属于正式兼容承诺。增加新组合时，必须同时更新工作流、支持版本表、README 和 Wiki。

## 2. 结果来源

1. GitHub Actions 是合并和发布判定的唯一正式测试来源。
2. 本地测试用于开发反馈，不代替 GitHub 的质量门禁，也不作为发布成功的证据。
3. 必须使用仓库工作流生成的日志、测试报告和构建产物定位失败，不能仅通过重复运行来忽略可复现故障。
4. 工作流必须使用仓库锁定的 Gradle Wrapper，不允许依赖运行器预装的全局 Gradle。

## 3. 触发规则

| 场景 | GitHub 工作流 | 要求 |
| --- | --- | --- |
| 推送到任意分支 | `持续集成` | 自动运行当前版本的构建、单元测试和完整 Java 运行矩阵 |
| 拉取请求 | `持续集成` | 自动运行；`质量门禁` 必须成功后才能合并到正式分支 |
| 人工复核 | `持续集成` | 通过 Actions 页面手动触发指定分支 |
| 周期巡检 | `全版本巡检` | 每周一 02:00（北京时间）自动检查全部维护版本，也支持手动触发 |
| 版本标签 | `自动发布` | 重新构建全部产物并执行完整 Java 运行矩阵；任一失败不得创建 Release |

## 4. 必须执行的检查

### 4.1 编译与单元测试

- 必须执行 `clean build`，不得只编译单一源集。
- 必须编译 `common` 代码和该 Minecraft 版本的全部正式加载器模块。
- 所有单元测试必须成功，测试报告无论成功或失败都应上传到 GitHub Actions。
- 必须使用表中规定的构建 JDK，确保 1.20.1 不会意外生成高于 Java 17 的字节码。

### 4.2 正式 JAR

- 运行测试必须使用 `remapJar` 生成的客户可安装 JAR，不能使用开发 JAR、源码 JAR 或测试夹具。
- 每个 Minecraft 版本必须恰好生成两个正式加载器 JAR。
- 构建产物必须上传为 GitHub Actions Artifact；正式发布还必须生成 GitHub 构建来源证明和 `SHA256SUMS.txt`。
- Fabric、Forge 和 NeoForge 产物不得要求兼容表之外的额外前置模组。

### 4.3 真实服务端运行测试

表中的每个“Minecraft × 加载器 × 运行 JDK”组合都必须：

1. 在新的临时目录安装对应的生产服务端。
2. 接受 EULA，并仅安装本次构建的一个 McBot 正式 JAR。
3. 确认实际 Java 主版本与矩阵声明一致。
4. 启动服务端并等待日志出现 Minecraft 的 `Done` 状态。
5. 确认日志记录 McBot 已加载，且记录了预期 Java 版本。
6. 通过标准输入发送 `stop`，并在规定时间内以退出码 0 正常关闭。
7. 检查日志中不存在类版本、模块访问、链接、初始化、缺失类、配置加载或崩溃类错误。

具体安装、超时和错误模式由 [`.github/scripts/test-java-runtime.ps1`](.github/scripts/test-java-runtime.ps1) 实现。修改检测脚本时必须经过相同的持续集成门禁。

## 5. 质量门禁

- 每个版本分支必须提供名称固定为 `质量门禁` 的汇总检查。
- `质量门禁` 只有在构建/单元测试和该分支全部运行矩阵都成功时才能成功。
- `1.20.1` 和 `1.21.1` 正式分支必须在 GitHub 分支保护中要求 `质量门禁`，并要求分支为最新状态。
- 取消、跳过、超时与失败具有相同的阻止效果。
- 紧急修复不得删除或弱化测试来获得绿色结果；如确需临时调整兼容承诺，必须先建立 GitHub Issue，说明影响、回退方案和恢复条件，并同步修改公开文档。

## 6. 发布门禁

发布标签必须采用无前缀的 `主版本.次版本.修订号` 格式，例如 `3.0.4`，并满足以下条件：

1. 标签版本与两个维护分支的 `mod_version` 一致。
2. 两个分支最近一次 `质量门禁` 均成功。
3. `自动发布` 工作流重新执行全部构建和真实服务端运行测试。
4. 四个正式 JAR、`SHA256SUMS.txt` 和构建来源证明均成功生成。
5. README、`SUPPORTED_VERSIONS.md`、更新日志和 Wiki 已同步到待发布版本。
6. Release 不是草稿或预发布版本，并可通过公开链接下载。

任一条件失败时不得手工上传未经同一工作流验证的替代 JAR。

## 7. 证据保留

- 普通构建 JAR：至少保留 14 天。
- 单元测试报告与普通服务端日志：至少保留 7 天。
- 发布门禁日志与发布构建 Artifact：至少保留 30 天。
- 正式 Release、SHA-256 清单和构建来源证明：按 GitHub Release 长期保留。

服务端运行失败时必须上传 `logs/` 和 `crash-reports/`，即使前序步骤失败也应尽可能保留诊断信息。

## 8. 故障处理

1. 首次失败由维护者检查失败任务、服务端日志和测试报告。
2. 网络下载等外部瞬时故障可以使用工作流内置的有限次数重试；业务断言失败不得自动忽略。
3. 能稳定复现的缺陷必须建立或关联 GitHub Issue，并记录受影响矩阵。
4. 修复必须由新的提交触发完整质量门禁，不能用修改旧日志或手工替换 Artifact 的方式结案。
5. 定时巡检失败但正式分支未发生变化时，同样视为兼容性回归，必须调查上游加载器、JDK 或下载源变化。

`全版本巡检` 失败时，工作流会自动创建或更新标题为 `[自动巡检] 全版本测试失败` 的 GitHub Issue，并附上失败矩阵结果与工作流链接。

## 9. 规范变更

对本规范、测试矩阵、错误判定或门禁条件的修改必须：

- 通过提交或拉取请求保留 Git 历史；
- 同步修改相关工作流和 Wiki；
- 通过修改后的 GitHub 质量门禁；
- 不得通过降低现有正式兼容范围来掩盖未解决的回归。

## 10. 对应实现

- 分支持续集成：[`.github/workflows/build.yml`](.github/workflows/build.yml)
- 全版本定时/手动巡检：[默认分支中的 `.github/workflows/repository-test.yml`](https://github.com/Minecraft0122/McBot-Reloaded/blob/1.20.1/.github/workflows/repository-test.yml)
- 自动发布门禁：[默认分支中的 `.github/workflows/publish.yml`](https://github.com/Minecraft0122/McBot-Reloaded/blob/1.20.1/.github/workflows/publish.yml)
- 真实服务端测试：[`.github/scripts/test-java-runtime.ps1`](.github/scripts/test-java-runtime.ps1)
- 公开兼容范围：[默认分支中的 `SUPPORTED_VERSIONS.md`](https://github.com/Minecraft0122/McBot-Reloaded/blob/1.20.1/SUPPORTED_VERSIONS.md)
